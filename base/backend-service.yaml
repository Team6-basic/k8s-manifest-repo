---
apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: backend-service
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: "/"
spec:
  selector:
    app: backend-fargate
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
